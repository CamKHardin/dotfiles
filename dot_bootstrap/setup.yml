---
- name: Machine setup
  hosts: localhost
  become: true
  connection: local
  gather_facts: true
  vars:
    linux_packages_by_family:
      RedHat:
        - git
        - htop
        - gh
        - gcc
        - ripgrep
        - zsh
        - ffmpeg
        - uv
        - fzf
        - tmux
        - make
        - patch
        - wl-clipboard
        - fd-find
        - gcc-c++
        - helix
        - autoconf
        - libiodbc
        - toilet
        - unar
      Debian:
        - git
        - htop
        - gh
        - ca-certificates
        - curl
        - gnupg
        - build-essential
        - ripgrep
        - zsh
        - ffmpeg
        - fzf
        - tmux
        - make
        - patch
        - xclip
        - fd-find
        - autoconf
        - libiodbc2
        - toilet
        - unar
      Archlinux:
        - git
        - htop
        - github-cli
        - gcc
        - ripgrep
        - zsh
        - ffmpeg
        - fzf
        - tmux
        - make
        - patch
        - wl-clipboard
        - fd
        - helix
        - autoconf
        - unixodbc
        - toilet
        - unar
      Suse:
        - git
        - htop
        - gh
        - gcc
        - ripgrep
        - zsh
        - ffmpeg
        - fzf
        - tmux
        - make
        - patch
        - wl-clipboard
        - fd
        - helix
        - autoconf
        - libiodbc
        - toilet
        - unar
    macos_packages:
      - git
      - htop
      - gh
      - ripgrep
      - zsh
      - ffmpeg
      - uv
      - fzf
      - tmux
      - fd
      - helix
      - unar
    zsh_shell_by_system:
      Linux: /usr/bin/zsh
      Darwin: /bin/zsh
    docker_arch_by_ansible_arch:
      x86_64: amd64
      aarch64: arm64
      armv7l: armhf

  tasks:
    - name: Get my user
      ansible.builtin.set_fact:
        remote_regular_user: "{{ ansible_env.SUDO_USER or ansible_user_id }}"

    - name: Lookup user account details
      ansible.builtin.getent:
        database: passwd
        key: "{{ remote_regular_user }}"

    - name: Set user paths
      ansible.builtin.set_fact:
        remote_regular_user_home: "{{ getent_passwd[remote_regular_user][4] }}"
        zsh_shell_path: "{{ zsh_shell_by_system[ansible_system] | default('/usr/bin/zsh') }}"

    - name: Install Linux packages
      ansible.builtin.package:
        name: "{{ linux_packages_by_family[ansible_os_family] }}"
        state: present
      when:
        - ansible_system == "Linux"
        - ansible_os_family in linux_packages_by_family

    - name: Ensure brew exists on macOS
      ansible.builtin.command: command -v brew
      register: brew_exists
      changed_when: false
      failed_when: brew_exists.rc != 0
      when: ansible_system == "Darwin"

    - name: Install macOS packages
      ansible.builtin.shell: |
        if brew list --formula {{ item }} >/dev/null 2>&1; then
          exit 0
        fi
        brew install {{ item }}
      args:
        executable: /bin/bash
      loop: "{{ macos_packages }}"
      changed_when: false
      when: ansible_system == "Darwin"

    - name: Check zsh binary
      ansible.builtin.stat:
        path: "{{ zsh_shell_path }}"
      register: zsh_shell_stat

    - name: Change shell to zsh
      ansible.builtin.user:
        name: "{{ remote_regular_user }}"
        shell: "{{ zsh_shell_path }}"
      when: zsh_shell_stat.stat.exists

    - name: Add Brave Browser Repo (Fedora)
      ansible.builtin.yum_repository:
        name: brave-browser
        description: Brave Browser
        baseurl: https://brave-browser-rpm-release.s3.brave.com/x86_64/
        gpgkey: https://brave-browser-rpm-release.s3.brave.com/brave-core.asc
        gpgcheck: true
        enabled: true
      when: ansible_distribution == "Fedora"

    - name: Import Brave Browser GPG Key (Fedora)
      ansible.builtin.rpm_key:
        key: https://brave-browser-rpm-release.s3.brave.com/brave-core.asc
        state: present
      when: ansible_distribution == "Fedora"

    - name: Install Brave Browser (Fedora)
      ansible.builtin.dnf:
        name: brave-browser
        state: present
      when: ansible_distribution == "Fedora"

    - name: Add Brave Browser keyring (Debian/Ubuntu)
      ansible.builtin.get_url:
        url: https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
        dest: /usr/share/keyrings/brave-browser-archive-keyring.gpg
        mode: "0644"
      when:
        - not ansible_check_mode
        - ansible_os_family == "Debian"

    - name: Add Brave Browser repo (Debian/Ubuntu)
      ansible.builtin.apt_repository:
        repo: "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main"
        filename: brave-browser-release
        state: present
      when: ansible_os_family == "Debian"

    - name: Install Brave Browser (Debian/Ubuntu)
      ansible.builtin.apt:
        name: brave-browser
        state: present
        update_cache: true
      when:
        - not ansible_check_mode
        - ansible_os_family == "Debian"

    - name: Add Docker repo (Fedora)
      ansible.builtin.yum_repository:
        name: docker
        description: Docker repo
        baseurl: "https://download.docker.com/linux/fedora/{{ ansible_distribution_major_version }}/{{ ansible_architecture }}/stable"
        gpgkey: https://download.docker.com/linux/fedora/gpg
        gpgcheck: true
        enabled: true
      when: ansible_distribution == "Fedora"

    - name: Install Docker (Fedora)
      ansible.builtin.dnf:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
      when: ansible_distribution == "Fedora"

    - name: Set Docker apt architecture
      ansible.builtin.set_fact:
        docker_apt_arch: "{{ docker_arch_by_ansible_arch.get(ansible_architecture, ansible_architecture) }}"
      when: ansible_os_family == "Debian"

    - name: Ensure apt keyrings directory exists
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        mode: "0755"
      when: ansible_os_family == "Debian"

    - name: Add Docker GPG key (Debian/Ubuntu)
      ansible.builtin.get_url:
        url: "https://download.docker.com/linux/{{ ansible_distribution | lower }}/gpg"
        dest: /etc/apt/keyrings/docker.asc
        mode: "0644"
      when:
        - not ansible_check_mode
        - ansible_os_family == "Debian"
        - ansible_distribution in ["Debian", "Ubuntu"]

    - name: Add Docker repo (Debian/Ubuntu)
      ansible.builtin.apt_repository:
        repo: "deb [arch={{ docker_apt_arch }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/{{ ansible_distribution | lower }} {{ ansible_distribution_release }} stable"
        filename: docker
        state: present
      when:
        - ansible_os_family == "Debian"
        - ansible_distribution in ["Debian", "Ubuntu"]

    - name: Install Docker (Debian/Ubuntu)
      ansible.builtin.apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: true
      when:
        - not ansible_check_mode
        - ansible_os_family == "Debian"
        - ansible_distribution in ["Debian", "Ubuntu"]

    - name: Add Docker group
      ansible.builtin.group:
        name: docker
        state: present
      when: ansible_system == "Linux"

    - name: Add user to docker group
      ansible.builtin.user:
        name: "{{ remote_regular_user }}"
        groups: docker
        append: true
      when: ansible_system == "Linux"

    - name: Set fonts directory
      ansible.builtin.set_fact:
        fonts_dir: "{{ remote_regular_user_home + '/Library/Fonts' if ansible_system == 'Darwin' else remote_regular_user_home + '/.fonts' }}"

    - name: Ensure fonts directory
      ansible.builtin.file:
        path: "{{ fonts_dir }}"
        state: directory
        mode: "0755"
        owner: "{{ remote_regular_user }}"

    - name: Check fonts directory state
      ansible.builtin.stat:
        path: "{{ fonts_dir }}"
      register: fonts_dir_stat

    - name: Check if JetBrains Mono exists
      ansible.builtin.find:
        paths: "{{ fonts_dir }}"
        patterns: "JetBrainsMonoNerd*FontMono*"
        file_type: file
      register: jetbrains_mono_files
      when: fonts_dir_stat.stat.isdir | default(false)

    - name: Download JetBrains Mono
      ansible.builtin.unarchive:
        src: https://github.com/ryanoasis/nerd-fonts/releases/download/v3.1.1/JetBrainsMono.zip
        dest: "{{ fonts_dir }}/"
        remote_src: true
        mode: "0755"
        owner: "{{ remote_regular_user }}"
      when:
        - not ansible_check_mode
        - fonts_dir_stat.stat.isdir | default(false)
        - jetbrains_mono_files.matched | int == 0
